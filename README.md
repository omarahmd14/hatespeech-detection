# hatespeech-detection
Hatespeech detection system that includes a vector database that uses FAISS for similarity search and implements a k-NN classifier and an LLM-based classifier using Retrieval-Augmented Generation (RAG) with Google Gemini.
